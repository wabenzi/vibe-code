# Security Configuration
# This file contains security-related environment variables and configuration

# API Security
VALID_API_KEY=your-secure-api-key-here-change-in-production
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# Production Security
NODE_ENV=development
SUPPRESS_ERROR_DETAILS=false

# Rate Limiting (configured in CDK)
API_RATE_LIMIT=100
API_BURST_LIMIT=200
API_QUOTA_LIMIT=10000

# Logging & Monitoring
LOG_LEVEL=INFO
SECURITY_LOG_LEVEL=INFO

# Environment-specific CORS
# Development
DEV_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080
# Production (update with your actual domain)
PROD_ALLOWED_ORIGINS=https://yourdomain.com,https://api.yourdomain.com

# DynamoDB Security
DYNAMODB_TABLE_NAME=users-table

# CloudWatch Log Retention (days)
LOG_RETENTION_DAYS=30
